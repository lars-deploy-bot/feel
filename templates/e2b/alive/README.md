# alive (E2B Template)

Full development template mirrored from infra `templates/alive/Dockerfile`.

Use this as the larger interactive/dev image.  
For E2E lifecycle checks, use `templates/e2b/alive-e2e-minimal/e2b.Dockerfile`.
