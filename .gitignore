# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-debug.log*
bun.lockb

# Environment variables (all contain secrets - never commit!)
.env
.env.local
.env.development
.env.staging
.env.test
.env.production
.env.*.local

# Environment templates (safe to commit - no secrets)
!.env.example
!.env.*.example

# Backup files (may contain secrets)
*.backup
*.bak

# Next.js
.next/
.next.dev-backup/
out/
build/
dist/
dist.*/
next-env.d.ts

# Generated build info (regenerated on each build/dev start)
apps/web/lib/build-info.json

# Turborepo
.turbo/

# Production builds (root level only)
/.next/
/out/
/build/
/dist/
/.builds/

# Runtime data
pids
*.pid
*.seed
*.pid.lock
.rate-limit-state.json

# Testing
coverage/
*.lcov
test-results/
playwright-report/
blob-report/
playwright/.cache/
.vitest/

# Logs
logs
*.log

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# nyc test coverage
.nyc_output

# Grunt intermediate storage
.grunt

# Bower dependency directory
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons
build/Release

# Dependency directories
node_modules/
jspm_packages/

# TypeScript cache
*.tsbuildinfo

# Compiled build outputs from packages (keep .ts source files only)
packages/deploy-scripts/src/*.js
packages/deploy-scripts/src/*.js.map
packages/deploy-scripts/src/*.d.ts
packages/deploy-scripts/deploy-scripts

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# parcel-bundler cache
.cache
.parcel-cache

# Nuxt.js build / generate output
.nuxt

# Gatsby files
.cache/
public

# Storybook build outputs
.out
.storybook-out

# Temporary folders
tmp/
temp/
.tmp/

# Local development workspace
.alive/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
domain-passwords.json
feedback.json

# Cryptographic keys (NEVER COMMIT)
*-private-key.json
*.pem
*.key
*.p12
*.pfx

# Ignore circular symlinks in packages
packages/*/guides
packages/*/images
packages/*/template
packages/*/tools
apps/web/.env.save
apps/shell-server/.env*
.env.broker

# Generated routing files (created by routing:generate script)
# These are server-specific and should not be committed
# Location: /var/lib/claude-bridge/generated/
.uploads/
domain-passwords.json.lock

# Live Caddy configurations (contain customer domains)
# Keep example files, ignore production configs
ops/caddy/Caddyfile
ops/caddy/Caddyfile.main
ops/caddy/Caddyfile.restore_backup
ops/caddy/Caddyfile.shell
!ops/caddy/Caddyfile.example
!ops/caddy/Caddyfile.snippets
!ops/caddy/etc/

# Agent memory database (development context)
use_this_to_remember.db

# Operations infrastructure (ops-only, never distribute)
# These are server management scripts, not part of the npm package
ops/scripts/site-modes.json
/var/lib/webalive/
/var/log/webalive-*.log
