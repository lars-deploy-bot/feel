# Server hardening - disable password auth, reduce attack surface
# Install: cp ops/ssh/hardening.conf /etc/ssh/sshd_config.d/hardening.conf && systemctl reload ssh
PasswordAuthentication no
KbdInteractiveAuthentication no
PermitEmptyPasswords no
MaxAuthTries 3
