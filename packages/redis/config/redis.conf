# Network
bind 0.0.0.0
port 6379

# Security
# Development password - use strong passwords in production
requirepass dev_password_only

# Persistence
save 900 1
save 300 10
save 60 10000
dbfilename dump.rdb
dir /data

# Memory
maxmemory 256mb
maxmemory-policy allkeys-lru

# Logging
loglevel notice
