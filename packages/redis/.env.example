# Redis connection URL
# Format: redis://[username]:[password]@[host]:[port]
# Development default includes password authentication
REDIS_URL=redis://:dev_password_only@127.0.0.1:6379

# Note: Password matches config/redis.conf requirepass setting
# For production, use a strong password and update both files
