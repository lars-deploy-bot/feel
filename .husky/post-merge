#!/bin/sh
# .husky/post-merge

bun run sync:skills

# Auto-install if lockfile changed during the merge
if git diff-tree -r --name-only ORIG_HEAD HEAD | grep -q '^bun.lock$'; then
    echo "📦 bun.lock changed — running bun install..."
    bun install
fi
