# Broker Environment Variables
#
# Copy to .env.broker and fill in values:
#   cp apps/broker/.env.example .env.broker
#
# The systemd service loads from /root/webalive/alive/.env.broker

# Required: Shared secret for internal authentication between Next.js and broker
# Generate with: openssl rand -hex 32
BROKER_SHARED_SECRET=your-secret-here

# Optional: Override port and host
# BROKER_PORT=3001
# BROKER_HOST=127.0.0.1
